# Lazy images + CLS demo

Illustrates images with/without dimensions.

## Commands
```bash
npm install
npm run demo
npm run typecheck
```

## Try
- Change width/height; set `loading="eager"` and see difference.
