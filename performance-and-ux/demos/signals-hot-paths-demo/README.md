# Signals in hot paths demo (mock)

Shows computed running only when source changes.

## Commands
```bash
npm install
npm run demo
npm run typecheck
```

## Try
- Set same value twice to see no extra recompute.
- Change to a new value and observe recompute.
