# dynamic import + preloading demo

Mocks preloading two feature bundles.

## Commands
```bash
npm install
npm run demo
npm run typecheck
```

## Try
- Add another feature to preload.
- Remove Promise.all to see sequential load.
