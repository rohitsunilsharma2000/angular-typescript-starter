# Toast + progress patterns demo

Logs a toast object transitioning from info to success with progress.

## Commands
```bash
npm install
npm run demo
npm run typecheck
```

## Try
- Add error state; reset progress to 0.
- Add timeout to auto-dismiss.
