# TestBed standalone demo (concept)

Prints the minimal TestBed setup for standalone components.

## Commands
```bash
npm install
npm run demo
npm run typecheck
```

## Try
- Add a provider/import string for your component.
