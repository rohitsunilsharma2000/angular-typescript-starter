# Generators & affected builds demo (mock)

Prints example Nx commands.

## Commands
```bash
npm install
npm run demo
npm run typecheck
```

## Try
- Swap command strings for your workspace (turbo filters, etc.).
