# PWA + Service Worker demo (mock)

Mocks a service worker update banner decision.

## Commands
```bash
npm install
npm run demo
npm run typecheck
```

## Try
- Flip `hasUpdate` or `registered` and rerun.
- Imagine hooking this into `SwUpdate` in Angular.
