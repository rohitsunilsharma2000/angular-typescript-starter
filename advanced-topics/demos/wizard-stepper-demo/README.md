# Wizard/stepper demo

Tiny state machine for next/prev.

## Commands
```bash
npm install
npm run demo
npm run typecheck
```

## Try
- Add a step and rerun.
- Guard next when validation fails (if you add validation flags).
