# Storybook setup demo (console mock)

Shows what a basic story meta + play might look like.

## Commands
```bash
npm install
npm run demo
npm run typecheck
```

## Try
- Change args (label/disabled) and rerun.
- Imagine this file as `*.stories.ts` — hook into real Storybook later.
