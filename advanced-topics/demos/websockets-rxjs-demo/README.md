# WebSockets with RxJS demo (simulated)

Uses an interval to mimic socket messages.

## Commands
```bash
npm install
npm run demo
npm run typecheck
```

## Try
- Increase `take` count to send more messages.
- Add `retry`/`catchError` to simulate reconnect.
