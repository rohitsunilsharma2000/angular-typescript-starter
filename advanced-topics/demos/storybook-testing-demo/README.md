# Storybook testing demo (console mock)

Shows a mock render + play assertion.

## Commands
```bash
npm install
npm run demo
npm run typecheck
```

## Try
- Change label to "Submit" and ensure assertion passes.
- Set `disabled=true` and log the output.
