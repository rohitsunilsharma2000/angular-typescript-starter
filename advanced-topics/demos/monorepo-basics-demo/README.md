# Monorepo (Nx/Turbo) basics demo (mock)

Lists sample projects and commands.

## Commands
```bash
npm install
npm run demo
npm run typecheck
```

## Try
- Add your own project name and nx command.
- Swap `nx` with `turbo run lint` etc.
