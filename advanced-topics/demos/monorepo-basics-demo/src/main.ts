const projects = ['patients-app', 'billing-lib', 'shared-ui'];
const commands = {
  lint: 'nx lint patients-app',
  test: 'nx test patients-app',
  graph: 'nx graph'
};
console.log('projects:', projects);
console.log('sample commands:', commands);
