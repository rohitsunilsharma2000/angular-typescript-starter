# IndexedDB wrapper demo (mock)

Promise-based API using Map to mimic IndexedDB put/get.

## Commands
```bash
npm install
npm run demo
npm run typecheck
```

## Try
- Add another store/key.
- Think how to swap Map with real IndexedDB wrapper (e.g., idb).
